[package]
name = "parch"
version = "0.2.0"
edition = "2024"
description = "A user-friendly wrapper over paru/pacman for Arch Linux"

[dependencies]
clap = { version = "4", features = ["derive"] }
which = "7"
